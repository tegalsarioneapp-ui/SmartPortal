Trigger file untuk peninjauan otomatis CodeRabbit.

File ini ditambahkan untuk memicu code review otomatis pada perubahan berikut:

1. Layout responsif: artifacts/smart-portal-rt/index.html sudah memiliki media queries untuk desktop dan mobile (@media max-width: 768px, 480px)

2. Sinkronisasi panel kanan: Fungsi window.loadWargaFbRight (baris 144-167) sudah diimplementasikan untuk:
   - Membaca data dari localStorage (saldo kas, info arisan, host)
   - Memperbarui elemen DOM panel kanan (#wfb-saldo-kas, #wfb-arisan, #wfb-host)
   - Dipanggil otomatis dari berbagai lifecycle hooks (DOMContentLoaded, BukaPortal, polling interval)

Fitur-fitur ini sudah berfungsi di kode saat ini dan tidak memerlukan perubahan tambahan.